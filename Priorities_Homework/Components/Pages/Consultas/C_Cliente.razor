@page "/ConsultaClientes"
@rendermode InteractiveServer

<h3><b>Consulta Clientes</b></h3>

@inject ClienteService clientesServices

<PageTitle>Consulta Clientes</PageTitle>
<h2><b>Lista de Clientes</b></h2>
<div class="table-responsive">
    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>ID</th>
                <th>Nombres</th>
                <th>RNC</th>
                <th>Teléfono</th>
                <th>Celular</th>
                <th>Email</th>
                <th>Dirección</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in ListaClientes) {
                <tr>
                    <td>@item.ClienteId</td>
                    <td>@item.Nombre</td>
                    <td>@item.RNC</td>
                    <td>@item.Telefono</td>
                    <td>@item.Celular</td>
                    <td>@item.Email</td>
                    <td>@item.Direccion</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@code {
    [Parameter]
    public Clientes clientes { get; set; } = new Clientes();
    public List<Clientes> ListaClientes { get; set; } = new List<Clientes>();

    protected override void OnInitialized() {
        ListaClientes = clientesServices.ObtenerLista(l => true);
    }
}
